export const tripHeadTemplate = `
<div class="trip__countdown">{{ countdown }}</div>
<h2 class="trip__destination">{{ name }}</h2>
<div class="trip__dates">
    <span class="trip__date trip__from">{{ from }}</span>
    <span class="trip__date trip__to">{{ to }}</span>
</div>`;